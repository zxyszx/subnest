# v0.3.34

- 修复 GitHub Release feed 先于附件可用导致页面显示“可更新但页面内更新不可用”的竞态，附件探测增加短暂重试。
- 发布流程新增 preflight，在 tag 发布前校验版本、发布说明和 Docker 部署附件。
- Cloudflare 生产凭据缺失时改为明确跳过部署并提示缺少的配置，不再让已成功的 GitHub Release 被最后一步误报为失败。
