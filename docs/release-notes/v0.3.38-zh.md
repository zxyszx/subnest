# v0.3.38

## 家庭共享与链接清理

- 家庭共享开关和 NewSzxcn 共享开关在请求期间显示加载状态，避免重复点击时看起来无响应。
- 输入 NewSzxcn 邮箱匹配期间显示明确的加载提示；普通邮箱仍保留手动填写收件链接的流程。
- 关闭家庭共享时清空已保存的邮箱、收件链接和加密凭据，避免停用后继续残留敏感配置。
- 删除订阅或更换/关闭已绑定邮箱时，自动撤销该邮箱对应的全部本地共享链接，并尽力撤销上游授权。

## 安全

- 共享链接撤销以本地代理状态为最终访问边界，旧短链接在删除或解绑后立即失效。
- 保持共享邮箱只读权限，管理入口继续通过受保护的设置页面访问。
