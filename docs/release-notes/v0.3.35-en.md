# v0.3.35

- Fix Docker runtime NewSzxcn connection tests incorrectly decoding mailbox and folder entries as nested arrays.
- Add regression coverage for paginated NewSzxcn mailbox and folder responses so connection tests return the mailbox list correctly.
