# SubNest v0.3.33

[中文 ->](./v0.3.33-zh.md)

Fixes Docker runtime testing NewSzxcn mailboxes always showing zero mailboxes.

- The Docker backend now uses the encrypted API token to fetch the real mailbox list.
- Adds mailbox and folder query endpoints.
- Keeps token masking and allows the token field to stay blank when changing other settings.
