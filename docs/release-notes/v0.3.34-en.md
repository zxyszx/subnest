# v0.3.34

- Fix the race where GitHub Release feed entries become visible before self-update assets are ready; asset probing now retries briefly.
- Add a release preflight that validates versions, release notes, and the Docker deployment artifact before publishing a tag.
- Skip Cloudflare production deployment with an explicit missing-credentials message instead of failing an otherwise successful release.
