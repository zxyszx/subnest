# v0.3.39

## Fixes

- Add the NewSzxcn mailbox matching and share-switch loading messages to the Chinese and English catalogs.
- Fix the Cloudflare release check blocked by untranslated UI text in `v0.3.38`.

This release also includes the family-sharing pending states, automatic link revocation, and sensitive configuration cleanup introduced in `v0.3.38`.
