# ------------------------------------------------------------
# 一键 Docker 部署配置（SubNest）
# ------------------------------------------------------------

# 对外服务端口。容器内部始终监听 3000。
PORT="3000"

# 镜像来源。SubNest 版本只从本仓库的 GitHub Container Registry 发布。
RENEWLET_IMAGE="ghcr.io/zxyszx/subnest:latest"

# Go 运行时软内存上限。小型 VPS/NAS 部署通常 128MiB 足够。
GOMEMLIMIT="128MiB"
MEM_LIMIT="256m"

# 容器时区。业务提醒时间仍按每个用户在应用内设置的时区计算。
TZ="Asia/Shanghai"

# 可选：Docker/Go 服务端主动访问上游 HTTP(S) 服务时使用的代理。
# 覆盖范围包括通知渠道、AI provider、GitHub Release/图标索引、WebDAV/S3 云备份等 HTTP 请求。
# 注意：容器内的 127.0.0.1/localhost 指向容器自身；宿主机代理请填写容器可访问的地址。
# 推荐写法：使用大写变量。
# HTTP_PROXY="http://host.docker.internal:7890"
# HTTPS_PROXY="http://host.docker.internal:7890"
# NO_PROXY="localhost,127.0.0.1,.local"
# 备选写法：也支持小写变量；不要同时配置两组，同时存在时 Go 优先读取大写。
# http_proxy="http://host.docker.internal:7890"
# https_proxy="http://host.docker.internal:7890"
# no_proxy="localhost,127.0.0.1,.local"

# 应用对外 URL，用于通知和密码重置邮件链接。
# 反向代理部署时，后续应改为你的 HTTPS 域名。
APP_URL="http://localhost:3000"

# 可选：标准 Docker Demo Mode。开启后使用固定 demo 账号登录，数据启动时和每 2 小时重置一次。
# Demo 登录：demo@renewlet.local / renewlet-demo
RENEWLET_DEMO_MODE="false"

# 可选：部署者可信的原始 <head> HTML 片段，UTF-8 最大 64 KiB；不需要时保持为空。
# Docker/Go 在进程启动时冻结；Vite dev 在启动时读取，Cloudflare Static Assets 在构建时读取。
# 片段拥有页面同源代码权限；启用后 CSP 切换为可信扩展策略，部署者负责第三方数据披露与 consent signal。
# 多行 Compose .env 与完整 Clarity 示例见 README，变量值不得包含 Markdown 链接、反斜杠标签或 HTML 空格实体。
RENEWLET_CUSTOM_HEAD_HTML=""

# 首次安装时由 deploy/docker-deploy.sh 生成。
# PB_ENCRYPTION_KEY 必须正好 32 个字符；不要随意轮换。
PB_ENCRYPTION_KEY=""
# CRON_SECRET 是外部通知 cron 调用使用的 Bearer token。
CRON_SECRET=""

# 可选：启用 PocketBase 密码重置邮件。
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_USER="smtp-user"
# SMTP_PASSWORD="smtp-password"
# SMTP_FROM="Renewlet <noreply@example.com>"
# SMTP_TLS="false"

# 可选：启用 PocketBase 自动备份，例如每天 03:00 执行并保留 7 份。
# BACKUPS_CRON="0 3 * * *"
# BACKUPS_CRON_MAX_KEEP="7"

# 内置通知调度器。普通 Docker/VPS 托管应保持启用。
NOTIFICATION_SCHEDULER_ENABLED="true"
NOTIFICATION_SCHEDULER_CRON="* * * * *"
NOTIFICATION_CRON_WINDOW_MINUTES="2"
NOTIFICATION_MAX_RETRIES="3"
NOTIFICATION_STALE_SENDING_MINUTES="15"

# 数据存储在 ./data，并挂载到容器内的 /pb_data。
